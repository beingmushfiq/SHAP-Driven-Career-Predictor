# Django Settings
DJANGO_SECRET_KEY=generate-a-secure-key-using-secrets-module
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Paths (defaults are automatically detected if not set)
# PROJECT_ROOT=  # Leave blank to auto-detect current project directory
DATA_RAW_DIR=data/raw
DATA_PROCESSED_DIR=data/processed
MODELS_DIR=models
MEDIA_DIR=webapp/media

# ML Configuration
RANDOM_SEED=42
TEST_SIZE=0.2
SHAP_BACKGROUND_SIZE=200
TUNING_N_ITER=50
TUNING_CV_FOLDS=5
OPTUNA_N_TRIALS=100
